# Normalize line endings to LF in the repo (avoids CRLF/LF churn on Windows).
* text=auto eol=lf

# Binary assets — never normalize.
*.lockb binary
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.webp binary
